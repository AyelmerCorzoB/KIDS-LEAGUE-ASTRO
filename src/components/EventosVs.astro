<div class="relative w-full max-w-7xl mx-auto px-4 pt-35 pb-26 md:py-0 md:pt-28">
  <!-- Contenido principal -->
  <div class="flex flex-col justify-center items-center px-4">
    <!-- Contenedor principal del partido -->
    <div
      class="flex flex-col md:flex-row items-center justify-center gap-8 md:gap-12 max-w-6xl mx-auto"
    >
      <!-- Jugador 1 (izquierda) -->
      <div class="hidden md:block">
        <img
          src="/Images/image 11 (1).png"
          alt="Jugador Equipo A"
          class="w-32 md:w-40 lg:w-48 transform transition-transform duration-500 hover:scale-105"
        />
      </div>

      <!-- Contenido central (VS) -->
      <div class="flex flex-col items-center text-center">
        <!-- Nombres de equipos y VS -->
        <div class="mb-4">
          <h2 class="text-white text-xl md:text-2xl font-bold tracking-wide">
            VIERNES 23 NOVIEMBRE <br />
            <span class="inline-block text-yellow-500 mt-2"
              >5:00 PM EN VIVO</span
            >
          </h2>
          <p class="text-white text-lg md:text-xl lg:text-2xl font-medium mt-2">
            <span class="block md:inline">KIDS LEAGUE A</span>
            <span
              class="text-yellow-300 text-4xl md:text-5xl lg:text-6xl font-bold block my-2 md:inline md:mx-4"
              >VS</span
            >
            <span class="block md:inline">KIDS LEAGUE B</span>
          </p>
        </div>

        <!-- Lugar -->
        <div class="mb-4">
          <p class="text-white text-base md:text-lg lg:text-xl">
            LUGAR:
            <span class="font-bold">CARRERA 23 #12</span><br />
            <span class="text-yellow-100">LA ESPERANZA</span>
          </p>
        </div>

        <!-- Balón (visible en todos los tamaños) -->
        <div class="mt-6 flex flex-col items-center justify-center space-y-4">
          <!-- Balón -->
          <img
            src="/Images/WhatsApp_Image_2024-11-05_at_9.00.37_AM-removebg-preview.svg"
            alt="Balón"
            class="w-16 md:w-20 animate-bounce"
          />

          <!-- Botón de Próximos Eventos -->
          <a
            href="/eventsfuture"
            class="inline-block bg-[#ff5722] text-white border-2 border-white px-6 py-2 rounded-full text-sm md:text-base font-bold shadow-lg hover:bg-[#ff9800] hover:text-black hover:border-[#ff5722] transform transition-all duration-300 hover:-translate-y-1 hover:shadow-xl active:scale-95"
          >
            PRÓXIMOS EVENTOS
          </a>
        </div>
      </div>

      <!-- Jugador 2 (derecha) -->
      <div class="hidden md:block">
        <img
          src="/Images/image.png"
          alt="Jugador Equipo B"
          class="w-32 md:w-40 lg:w-48 transform transition-transform duration-500 hover:scale-105"
        />
      </div>
    </div>
  </div>
</div>

<style>
  /* Añadir un fondo sutil si es necesario */
  .animate-bounce {
    animation: bounce 2s infinite;
  }

  @keyframes bounce {
    0%,
    100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-10px);
    }
  }
</style>
